version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: officetaskmanagement_web
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
    restart: unless-stopped
